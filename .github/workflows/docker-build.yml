#===============================================================================
# GitHub Actions: Build and Push Docker Container
#===============================================================================
# 
# This workflow automatically builds the Docker container when:
# - The Dockerfile is updated
# - A new release/tag is created
# - Manual trigger via workflow_dispatch
#
# The container is pushed to GitHub Container Registry (ghcr.io)
#
# Setup required:
# 1. Go to repository Settings > Actions > General
# 2. Enable "Read and write permissions" for GITHUB_TOKEN
# 3. (Optional) Add secrets for other registries (DOCKERHUB_USERNAME, DOCKERHUB_TOKEN)
#
#===============================================================================

name: Build and Push Docker Container

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'Dockerfile'
      - '.github/workflows/docker-build.yml'
      - 'requirements.txt'
  
  pull_request:
    branches:
      - main
      - master
    paths:
      - 'Dockerfile'
  
  release:
    types: [published]
  
  # Allow manual trigger
  workflow_dispatch:
    inputs:
      tag:
        description: 'Custom tag for the image (optional)'
        required: false
        default: ''

env:
  REGISTRY: ghcr.io
  # Docker requires lowercase image names
  IMAGE_NAME: andresgordoortiz/copenhagenworkflow

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: write
    
    steps:
      #=========================================================================
      # Free up disk space (GitHub runners have ~14GB, we need more)
      #=========================================================================
      - name: Free up disk space
        run: |
          echo "=== Before cleanup ==="
          df -h
          
          # Remove unnecessary large packages
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf /usr/share/swift
          
          # Remove pre-installed Docker images
          sudo docker image prune -a -f || true
          
          # Clean apt cache
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          
          echo "=== After cleanup ==="
          df -h

      #=========================================================================
      # Setup
      #=========================================================================
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up QEMU (for multi-platform builds)
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      #=========================================================================
      # Login to Container Registries
      #=========================================================================
      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      # Optional: Login to Docker Hub (uncomment if you want to push there too)
      # - name: Log in to Docker Hub
      #   if: github.event_name != 'pull_request'
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      #=========================================================================
      # Generate Tags and Labels
      #=========================================================================
      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Tag with 'latest' on main/master branch
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') || github.ref == format('refs/heads/{0}', 'master') }}
            # Tag with branch name
            type=ref,event=branch
            # Tag with PR number
            type=ref,event=pr
            # Tag with version on release
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            # Tag with SHA
            type=sha,prefix=sha-
            # Custom tag from manual trigger
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event.inputs.tag != '' }}
          labels: |
            org.opencontainers.image.title=CopenhagenWorkflow
            org.opencontainers.image.description=4D cell tracking and fate analysis pipeline with GPU support
            org.opencontainers.image.vendor=CopenhagenWorkflow Team
      
      #=========================================================================
      # Build and Push
      #=========================================================================
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Build only for amd64 (GPU containers are linux/amd64 only)
          platforms: linux/amd64
      
      #=========================================================================
      # Summary
      #=========================================================================
      - name: Image digest
        run: |
          echo "## Docker Image Built Successfully! 🐳" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Registry:** ${{ env.REGISTRY }}" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** ${{ env.IMAGE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Tags:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Pull Command:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Convert to Singularity:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "singularity pull copenhagen_workflow.sif docker://${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

  #=============================================================================
  # Test the built image
  #=============================================================================
  test-image:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Pull and test image
        run: |
          # Pull the image
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          
          # Test Python imports
          echo "Testing Python imports..."
          docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest python -c "
          import numpy
          print(f'NumPy: {numpy.__version__}')
          assert numpy.__version__.startswith('1.'), 'NumPy must be 1.x for compatibility'
          import tensorflow as tf
          print(f'TensorFlow: {tf.__version__}')
          import torch
          print(f'PyTorch: {torch.__version__}')
          from cellpose import version as cellpose_version
          print(f'Cellpose: {cellpose_version}')
          import stardist
          print(f'StarDist: {stardist.__version__}')
          print('Core packages OK!')
          import hydra
          print(f'Hydra: {hydra.__version__}')
          print('All imports successful!')
          "
          
          # Test Fiji (use full path)
          echo "Testing Fiji..."
          docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest /opt/Fiji.app/ImageJ-linux64 --headless --help 2>&1 | head -5 || echo "Fiji binary check"
          docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest ls -la /opt/Fiji.app/ImageJ-linux64 || echo "Fiji not found"
          
          echo "All tests passed!"
      
      - name: Test summary
        run: |
          echo "## Image Tests Passed ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The container was successfully tested with all core dependencies." >> $GITHUB_STEP_SUMMARY
